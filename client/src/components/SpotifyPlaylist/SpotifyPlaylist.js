import React, { Component } from "react";

const SpotifyPlaylist = props => (
  <div className="card">
    <h4 className="card-header bg-primary text-white">Current SpotifyPlaylist</h4>
  </div>
);

export default SpotifyPlaylist;
